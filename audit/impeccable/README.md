# Impeccable: Deep Audit

A deep technical audit of [github.com/pbakaus/impeccable](https://github.com/pbakaus/impeccable), read through the lens of **YoinkIt**. The goal was not "what is it" but "how does it work, deeply, and what can we steal." The short answer: Impeccable independently solved the three problems at the center of YoinkIt's architecture (understand an arbitrary website, run from a real browser, collaborate with an agent about it), plus a fourth YoinkIt has not tackled yet (ship one skill to many agent harnesses from a single source).

## Start here

1. [`00-EXECUTIVE-SUMMARY.md`](00-EXECUTIVE-SUMMARY.md): what Impeccable is, the three-layer architecture, the master diagram, the Impeccable↔YoinkIt correspondence, and the one framing you must hold before reading anything else (the inversion: same shape, opposite physics).
2. [`PATTERNS-FOR-YOINKIT.md`](PATTERNS-FOR-YOINKIT.md): the payoff. Every transferable pattern, organized by YoinkIt concern, each tagged STEAL / ADAPT / AVOID with `file:line` references and a concrete YoinkIt application. Ends with a ranked priority list.
3. [`diagrams/system-map.md`](diagrams/system-map.md): consolidated visuals, including a forward-looking "YoinkIt target architecture" diagram showing what adopting the top patterns would look like.
4. [`reports/`](reports/): the subsystem deep-dives. Full depth, written straight to disk so nothing was lost compressing through a summary; each report (01–05) fans out into a folder of focused sub-dives.
5. [`06-UNEXPLORED-TERRITORY.md`](06-UNEXPLORED-TERRITORY.md): the forward-looking companion. Where reports 01–05 map Impeccable onto **what YoinkIt is today**, this maps the half the audit skipped on purpose (the product surface, the proof machinery, the persisted artifact, the release spine) through a **generative** lens: not "what can we steal" but "what could YoinkIt become." Four ranked territories, led by a persisted **motion memory** and a falsifiable **fidelity proof**. **Territory 1 is now escalated** to a full deep-dive folder: [`reports/06-design-memory/`](reports/06-design-memory/06-design-memory.md).

## The subsystem deep-dive reports

| # | Report | One-line | Layer |
|---|---|---|---|
| 01 | [Detector engine](reports/01-detector-engine/01-detector-engine.md) | Deterministic no-LLM engine; one pure rule core runs across a Node cascade / Puppeteer / injected / regex; design-system extraction; a hand-rolled ~1000-line CSS cascade; fail-open completeness | Understanding |
| 02 | [Chrome extension](reports/02-chrome-extension/02-chrome-extension.md) | MV3 service-worker hub; engine generated then embedded; CSP two-tier injection ladder; MAIN-world bridge with a ready handshake; on-demand injection; SW-death survival | Surface |
| 03 | [Live mode](reports/03-live-mode/03-live-mode.md) | The crown jewel, end to end: the local-server collaboration loop **and** the in-page overlay / manual-edit round-trip. Long-poll + leased events + append-only session journal (crash-recoverable); SSE relay; self-stabilizing dual locator; carbonize two-phase commit; redundant-evidence + server-verified write-back. | Collaboration |
| 04 | [Skill + harness distribution](reports/04-skill-harness/04-skill-harness.md) | One `SKILL.src.md` + `reference/` tree compiled to 13 provider targets (12 committed install dirs) via a ~170-line `createTransformer` core over two config tables; placeholder substitution + harness-conditional blocks; one-skill/N-commands router; once-per-session context-boot protocol; command-metadata SSOT + build-enforced counts; the `pin` shim; committed-output distribution | Distribution |
| 05 | [Hook system](reports/05-hook-system/05-hook-system.md) | Provider-native hooks run the detector on every edit; post-edit surface vs pre-write block; "never break the turn" fail-open contract; anti-nag machinery; two-tier config + ask-once consent | Surface |
| 06 | [Design memory → motion memory](reports/06-design-memory/06-design-memory.md) | The persisted, git-tracked, versioned `.impeccable/design.json` with a `motion` block; the LLM generation path + v1→v2 migration; the enforcement reader that lints code against the memory within a tolerance; and a concrete, written-out **measured `motion.json`** proposal for YoinkIt. The first escalation from the unexplored-territory survey (Territory 1) — generative, not extractive | Memory |

**Detector deep dives.** Report 01 now has four companions that go below the overview and correct a few first-draft details: [`01a`](reports/01-detector-engine/01a-rule-trinity-and-dispatch.md) (the rule trinity, engine dispatch, and the full rule×engine matrix), [`01b`](reports/01-detector-engine/01b-css-cascade-engine.md) (the hand-rolled CSS cascade), [`01c`](reports/01-detector-engine/01c-color-and-contrast-tiers.md) (color science and the three-tier contrast escalation), and [`01d`](reports/01-detector-engine/01d-selector-and-footprint.md) (selector generation and footprint scrubbing). Start from the box at the top of [report 01](reports/01-detector-engine/01-detector-engine.md).

**Chrome extension deep dives.** Report 02 now has four companions that go below the overview and correct several first-draft and upstream-doc details: [`02a`](reports/02-chrome-extension/02a-manifest-permissions-build.md) (manifest, permissions, and the generate-then-embed build pipeline), [`02b`](reports/02-chrome-extension/02b-messaging-and-survival.md) (the messaging topology, the two-hop bridge vocabulary, and the MV3 service-worker-death survival kit), [`02c`](reports/02-chrome-extension/02c-injection-and-main-world.md) (MAIN-world injection, the CSP two-tier ladder, the ready-handshake race, idempotency, and footprint scrubbing), and [`02d`](reports/02-chrome-extension/02d-devtools-surfaces.md) (the panel/sidebar/popup surfaces, `inspect`/`$0` integration, and copy-as-markdown). Start from the box at the top of [report 02](reports/02-chrome-extension/02-chrome-extension.md).

**Live mode deep dives.** Report 03 is a folder: an overview plus six companions that go to the floor and **consolidate what the first pass split across two parallel drafts** (former reports 03 and 04), correcting the ADR's pre-carbonize accept flow, a missing `live-wrap.mjs`, the "state machine that is really a fold," and several stale line numbers. [`03a`](reports/03-live-mode/03a-server-transport-and-protocol.md) (the zero-dependency server, the long-poll + event-lease transport, the served-script handshake), [`03b`](reports/03-live-mode/03b-session-journal-and-recovery.md) (the append-only session journal, the snapshot fold, and crash recovery), [`03c`](reports/03-live-mode/03c-variant-lifecycle-and-carbonize.md) (the variant lifecycle and the carbonize two-phase commit), [`03d`](reports/03-live-mode/03d-overlay-picker-and-locators.md) (the in-page overlay, the element picker, and the id-decisive dual locator), [`03e`](reports/03-live-mode/03e-manual-edit-round-trip.md) (the untrusted-agent manual-edit write-back loop), and [`03f`](reports/03-live-mode/03f-framework-source-mapping.md) (mapping a live element back to source under component frameworks). Start from the box at the top of [report 03](reports/03-live-mode/03-live-mode.md). Report number **04 was the gap** left by that consolidation; it is now filled by the skill + harness deep dive below.

**Skill + harness deep dives.** Report 04 is a folder: an overview plus five companions that take the skill / multi-harness distribution subsystem (first drafted as report 05) to floor-level depth, re-verifying line numbers and correcting several first-draft details — the 11-entry placeholder table and its `agents→codex / github→agents / trae-cn→trae` borrow chain, the `SKILL_CATEGORIES`-vs-router-label taxonomy split, the curated 19-command `{{available_commands}}` list, the 23-name deprecated-skill cleanup, the no-op `validateSiteHeader` stub, and the dead `codex-toml` branch. [`04a`](reports/04-skill-harness/04a-single-source-transform.md) (the one-source → N-harness transform), [`04b`](reports/04-skill-harness/04b-build-pipeline-and-validators.md) (build orchestration, agents, hooks, validators), [`04c`](reports/04-skill-harness/04c-runtime-routing-and-context.md) (the runtime router and context protocol), [`04d`](reports/04-skill-harness/04d-command-metadata-and-pin.md) (command-metadata SSOT, count enforcement, the pin shim), and [`04e`](reports/04-skill-harness/04e-distribution-and-install.md) (the distribution and install model). Start from the box at the top of [report 04](reports/04-skill-harness/04-skill-harness.md). Report number **05 is the hook-system deep dive** (below): the skill-harness draft that first held that slot was promoted into the report 04 folder, and report 05 was then reused for the hook system (first drafted as report 06).

**Hook system deep dives.** Report 05 is a folder: an overview plus five companions that take the hook system + per-project config model (first drafted as report 06) to floor-level depth, re-verifying line numbers and correcting several first-draft details — the off-by-one line counts, the full `DEFAULT_CONFIG` shape, the write-dead `hook.pending.json` tombstone, the two-shape-definitions (not "three copies") manifest-duplication census, the two divergent `.git/info/exclude` writers, and the three legacy hook-script marker tombstones. [`05a`](reports/05-hook-system/05a-hook-models-and-runtime-core.md) (the two hook models, the shared runtime core, and the never-break-the-turn fail-open contract), [`05b`](reports/05-hook-system/05b-anti-nag-and-the-directive.md) (the dedup cache, edit-count suppression, the Cursor denial loop-breaker, and the directive footer), [`05c`](reports/05-hook-system/05c-config-and-ignore-model.md) (the two-tier config, the three ignore axes, consent storage, and the git-exclude writers), [`05d`](reports/05-hook-system/05d-admin-cli-and-contract.md) (the `/impeccable hooks` CLI, runtime manifest repair, and the intentional-findings policy), and [`05e`](reports/05-hook-system/05e-manifest-generation-and-install.md) (manifest generation, the four placement paths, and the consenting install). Start from the box at the top of [report 05](reports/05-hook-system/05-hook-system.md). With live mode at 03, skill + harness at 04, and the hook system here at 05, the report sequence is now gapless (01–05).

**Design-memory deep dives.** Report 06 is the first **escalation from the [unexplored-territory survey](06-UNEXPLORED-TERRITORY.md)**: it takes that survey's Territory 1 (the persisted design-memory) to floor-level depth, holding the audit's inversion the whole way (Impeccable's memory is **prescriptive and authored**; YoinkIt's would be **measured and observed**). An overview plus four companions: [`06a`](reports/06-design-memory/06a-the-persisted-artifact.md) (the full `design.json` schema, the three-file `.impeccable/` directory, versioning, and the real-≠-demo-≠-spec looseness), [`06b`](reports/06-design-memory/06b-generation-and-migration.md) (the LLM generation path, synthesize-on-thin, the v1→v2 migration, and the correction that no reader branches on `schemaVersion` and `mdNewerThanJson` lives in the readers, not `document.md`), [`06c`](reports/06-design-memory/06c-the-enforcement-reader.md) (`design-system.mjs` traced end to end — the allowed-set/tolerance linter, the live-server merge, the Register one-field conditioner), and [`06d`](reports/06-design-memory/06d-a-motion-json-for-yoinkit.md) (**the payoff**: a literal `.yoinkit/motion.json` schema, how a `__cap.dump()` spec folds in, accumulation, free interop with `extensions.motion`, the motion `narrative` and `register` tag, and the motion-consistency linter — `design-system.mjs` with its truth-source inverted). Start from the box at the top of [report 06](reports/06-design-memory/06-design-memory.md). Each sub-dive re-verifies its line numbers against `source/` and collects the survey's stale specifics in a corrections block.

## What this audit concluded (one paragraph)

The instinct behind the audit was correct. Impeccable is the closest existing reference implementation of YoinkIt's architecture, built by someone who hit the same walls (selector drift across reloads, CSP, MAIN-world injection, arming before the engine loaded, agent latency, multi-harness duplication) and shipped solutions to all of them. The highest-value, lowest-risk takeaway is single-source skill distribution, which would delete YoinkIt's hand-maintained `skill/codex` and `skill/claude` copies. The deepest architectural takeaway is the collaboration loop: long-poll as a harness-agnostic agent transport, SSE to the browser, and an append-only journal that makes the loop crash-recoverable. The most directly droppable code is the selector generation and dual-locator re-resolution, which slot straight into `pick()` and `on(sel)`. The one thing to internalize before copying anything is that Impeccable writes code into the user's repo and treats capture as free, whereas YoinkIt emits a spec and treats the real-browser capture as the hard part: same shape, opposite physics, so steal the state machines and the transport, not the file-writing or the relaxed capture assumptions.

## Provenance and method

- **Target:** `github.com/pbakaus/impeccable`, shallow-cloned to `source/` (gitignored, not part of this repo's history). About 2,059 tracked files; the signal is concentrated in `skill/`, `extension/`, `cli/`, `scripts/`, and the root docs.
- **Audited:** 2026-06-18, against the then-current `main`.
- **Method:** six parallel deep-dive agents, each owning one subsystem and writing its full-depth report straight to [`reports/`](reports/). This synthesis layer (executive summary, patterns, diagrams, this index) was authored on top of those persisted reports. Reports 01–05 were since taken to floor-level depth as folders of sub-dives; the original split live-mode pair (first-pass reports 03 + 04) was consolidated into [`reports/03-live-mode/`](reports/03-live-mode/), report number 04 was then reused for the skill + harness deep dive (first drafted as report 05, now [`reports/04-skill-harness/`](reports/04-skill-harness/)), and report number 05 was reused for the hook-system deep dive (first drafted as report 06, now [`reports/05-hook-system/`](reports/05-hook-system/)) — so the final sequence 01–05 is gapless.
- **A note on the reports:** they are research artifacts, dense and occasionally raw. Line numbers were accurate at audit time against the cloned `main`; re-verify against `source/` if a specific line matters, since the upstream repo moves. The CLAUDE.md inside the upstream repo is itself stale in places (for example it references line numbers in `cli/engine/detect-antipatterns.mjs` that is now a re-export facade); report 01 notes where.
